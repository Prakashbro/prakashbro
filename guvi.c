#include<stdio.h>
int main()
{
int num;
/*number from user*/
print f(Enter the Number);
("%d",&num);
if (num > 0)
{
printf (number is positive);
}
else if(num < 0)
{
printf (Number Is Negative);
}
else
{
print f(number is zero);
}
return 0;
}
